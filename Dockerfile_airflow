FROM "puckel/docker-airflow"

ADD ./requirements.txt requirements.txt
RUN pip install --user -r requirements.txt

ADD ./pavement.py pavement.py
ADD ./dags dags
ADD ./test test

ADD ./my_token .cache-1237967510
ENV PATH "/usr/local/airflow/.local/bin:$PATH"

ENTRYPOINT ["paver"]
CMD ["test_dags"]
